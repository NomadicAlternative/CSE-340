<h1><%- title %></h1>

<%- messages() %>

<p class="delete-warning">Confirm Deletion - The delete is permanent.</p>

<section class="delete-inventory">
  <div class="form-card wide">
    <form id="deleteForm" class="inventory-form" method="post" action="/inv/delete">
      
      <input type="hidden" name="inv_id"
      <% if(locals.inv_id) { %> value="<%= locals.inv_id %>"
      <% } %>>

      <div class="form-group">
        <label for="inv_make">Make</label>
        <input 
          type="text" 
          id="inv_make" 
          name="inv_make" 
          readonly
          value="<%= locals.inv_make || '' %>"
        >
      </div>

      <div class="form-group">
        <label for="inv_model">Model</label>
        <input 
          type="text" 
          id="inv_model" 
          name="inv_model" 
          readonly
          value="<%= locals.inv_model || '' %>"
        >
      </div>

      <div class="form-group">
        <label for="inv_year">Year</label>
        <input 
          type="text" 
          id="inv_year" 
          name="inv_year" 
          readonly
          value="<%= locals.inv_year || '' %>"
        >
      </div>

      <div class="form-group">
        <label for="inv_price">Price</label>
        <input 
          type="text" 
          id="inv_price" 
          name="inv_price" 
          readonly
          value="<%= locals.inv_price || '' %>"
        >
      </div>

      <button class="btn-danger" type="submit">Delete Vehicle</button>
    </form>
    <p class="form-footnote"><a href="/inv/">Back to Management</a></p>
  </div>
</section>
